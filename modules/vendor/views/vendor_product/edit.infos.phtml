<div class="form padding_1">
    <form method="post" autocomplete="off" q-event="submit" q-object="edit_infos" q-static="<?php echo H(array('id'=>$product->id))?>">
        <?php echo V('form_error', array('form'=>$form));?>
        <?php $require = (string) V('form_require'); ?>
        <table class="form flexible">
            <tbody>
                <tr>
                    <td class="label left nowrap">
                    <?php echo T('类别'); ?>
                    </td>
                    <td class="nowrap">
                    <?php
                        $types = Product_Model::get_types();
                        $type = $product->type;
                    ?>
                    <span class="padding_1">
                        <?php echo H($types[$type]);?>
                    </span>
                    <?php
                        $root = Product_Category_Model::root($type);
                        echo Widget::factory('application:category_selector', array(
                            'category' => $product->category,
                            'root'=>$root,
                            'name'=> 'category',
                            'ajax'=>TRUE
                        ));
                    ?>
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <input type="submit" name="submit" class="button button_add" value="<?php echo T('保存')?>" />
                    </td>
                </tr>
            </tbody>
        </table>
    </form>
</div>
