<?php 
    $full_path = Cache::cache_file($full_path, TRUE);
    $info = getimagesize($full_path);
	$width = $info[0] > 800 ? 800 : $info[0];
	$height = $info[0] > 800 ? (int)($info[1] * 800 / $info[0]) : $info[1];
?> 
<div class="padding_2">
    <img id="img1" src="<?php echo $full_path;?>" width="<?php echo $width?>" height="<?php echo $height?>" />
</div>
