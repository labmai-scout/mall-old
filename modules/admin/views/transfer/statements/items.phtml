<?php

foreach (Q("$statement order") as $order) {
	$links[] = URI::anchor($order->url(NULL, NULL, NULL, 'admin_view'), HT('#%ref_no', array('%ref_no'=>$order->order_no)), 'class="blue"');
}
echo implode(', ', $links);
